@model IEnumerable<Student>

@{
    ViewData["Title"] = "Student Catalog";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Register new Student</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfBirth)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var student in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => student.StudentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => student.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => student.DateOfBirth)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@student.StudentId">Edit</a>
                <a class="btn btn-primary" asp-action="Details" asp-route-id="@student.StudentId">Details</a>
                <a class="btn btn-primary" asp-action="Delete" asp-route-id="@student.StudentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
